# Configure pytest to find the `src` package without installing it
[pytest]
pythonpath = src
log_cli = true
log_cli_level = INFO
log_cli_format = %(levelname)s %(message)s
# Exclude scripts directory - contains utility scripts, not tests
norecursedirs = scripts